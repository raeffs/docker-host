services:
  mailpit:
    container_name: 'mailpit'
    image: 'axllent/mailpit:v1.30.6@sha256:7f33095f80e901f6ad08028f06ca284aa58fe84942be5496008d041d3b9f4d4d'
    restart: 'unless-stopped'
    user: '65023:65023'
    read_only: true
    mem_limit: 256M
    cap_drop:
      - 'ALL'
    security_opt:
      - 'no-new-privileges'
    networks:
      - 'default-frontend'
    tmpfs:
      - '/tmp'
    volumes:
      - '/etc/localtime:/etc/localtime:ro'
      - 'mailpit-data:/data'
    environment:
      MP_DATABASE: '/data/mailpit.db'
      MP_MAX_MESSAGES: '${MAILPIT_MAX_MESSAGES}'
      MP_MAX_AGE: '${MAILPIT_MAX_AGE}'
      MP_SMTP_AUTH_ACCEPT_ANY: 'true'
      MP_SMTP_AUTH_ALLOW_INSECURE: 'true'
      MP_UI_AUTH: '${MAILPIT_UI_USER}:${MAILPIT_UI_PASSWORD}'
      MP_DISABLE_VERSION_CHECK: 'true'
    healthcheck:
      test: ['CMD', '/mailpit', 'readyz']
      start_period: '10s'
      interval: '5s'
      timeout: '5s'
      retries: 3
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.mailpit.rule=Host(`${MAILPIT_DOMAIN}`)'
      - 'traefik.http.routers.mailpit.middlewares=default@file'
      - 'traefik.http.services.mailpit.loadbalancer.server.port=8025'

volumes:
  mailpit-data:
    external: true

networks:
  default-frontend:
    external: true
