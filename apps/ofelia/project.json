{
  "name": "ofelia",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "implicitDependencies": ["networks"],
  "targets": {
    "create-variables": {
      "options": {
        "variables": [
          {
            "name": "OFELIA_SLACK_URL",
            "description": "Slack webhook url used by Ofelia to post notifications."
          },
          {
            "name": "OFELIA_DOCKER_GID",
            "description": "GID of the docker group. The docker proxy will run under this GID to access the docker socket.",
            "type": "docker-group"
          }
        ]
      }
    }
  }
}
