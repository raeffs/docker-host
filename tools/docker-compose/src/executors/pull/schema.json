{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "cli": "nx",
  "type": "object",
  "properties": {
    "composeFile": {
      "type": "string",
      "default": "docker-compose.yml"
    }
  },
  "required": ["composeFile"]
}
