version: '3.8'

networks:
  default:
    external:
      name: docker-host-network

volumes:
  recipes-db:
    external: true

services:
  recipes:
    extends:
      file: 'recipes/service.yml'
      service: 'recipes'
    depends_on:
      - 'recipes-db'

  recipes-db:
    extends:
      file: 'recipes/service.yml'
      service: 'recipes-db'
