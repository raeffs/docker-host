version: '3.8'

networks:
  default:
    external:
      name: docker-host-network

services:
  pihole:
    extends:
      file: 'pihole/service.yml'
      service: 'pihole'

  unbound:
    extends:
      file: 'unbound/service.yml'
      service: 'unbound'
