log:
  filePath: '/var/log/traefik.log'
  level: 'INFO'

providers:
  docker:
    exposedByDefault: false
    watch: true
  file:
    directory: '/etc/traefik/dynamic'
    watch: true

api:
  insecure: true

entryPoints:
  web:
    address: :80
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https

  websecure:
    address: :443
